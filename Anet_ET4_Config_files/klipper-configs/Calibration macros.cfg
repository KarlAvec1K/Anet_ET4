# User-facing calibration helpers for the Mainsail Bed Calibration group.

[gcode_macro Z_OFFSET_CALIBRATE]
description: Start probe Z offset calibration
gcode:
    PROBE_CALIBRATE

[gcode_macro SAVE_CONFIGURATION]
description: Save the current Klipper configuration
gcode:
    SAVE_CONFIG
